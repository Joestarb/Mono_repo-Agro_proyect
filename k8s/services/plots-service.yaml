# Deployment del Plots Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: plots-service
  namespace: default
  labels:
    app: plots-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plots-service
  template:
    metadata:
      labels:
        app: plots-service
    spec:
      containers:
      - name: plots-service
        image: agro-plots-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef:
              name: agro-config
              key: POSTGRES_HOST
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: agro-config
              key: POSTGRES_PORT
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: agro-config
              key: POSTGRES_DB
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: agro-secrets
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: agro-secrets
              key: POSTGRES_PASSWORD
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: agro-config
              key: NODE_ENV
        - name: PORT
          value: "3000"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
      restartPolicy: Always
---
# Service del Plots Service
apiVersion: v1
kind: Service
metadata:
  name: plots-service
  namespace: default
  labels:
    app: plots-service
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: plots-service
